//coding-classes
#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n1,n2;
    cin>>n1;
    
    while(n1--)
    {
        int n;
        cin>>n;
        long int a[n+1]={0};
         long int sum[n+1]={0};
        for(int i=1;i<=n;i++)
        {
            cin>>a[i];
            #include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n1,n2;
    cin>>n1
    
    while(n1--)
    {
        int n;
        cin>>n;
        long int a[n+1]={0};
        long int sum[n+1]={0};
        for(int i=1;i<=n;i++)
        {
            cin>>a[i];
            sum[i]=sum[i-1]+a[i];

        }
        
        cin>>n2
        while(n2--)
        {
            int L,R;
            cin>>L>>R;
            cout<<sum[R]-sum[L-1]<<;
        }
    }
}];

        }
        
        cin>>n2;
        while(n2--)
        {
            int L,R;
            cin>>L>>R;
            cout<<sum[R]-sum[L-1]<<;
        }
    }
}
